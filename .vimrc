" Some basics:
	set nocompatible
	filetype plugin on
	syntax on
	set encoding=utf-8
	set number

" Splits open at the bottom and right, which is non-retarded, unlike vim defaults.
	set splitbelow
	set splitright
